ENV=develop

# サーバーPORT
PORT=8080

# DB設定
DB_HOST=mysql
DB_PORT=3306
DB_NAME=myapp
DB_USER=root
DB_PASS=password

# Auth/JWT settings
# 秘密鍵（開発用の例。必ず本番では強力な値に変更し、共有しない）
JWT_SECRET_KEY=dev-change-me-super-secret

# アクセストークン有効期限（時間）
ACCESS_TOKEN_TTL_HOURS=1

# リフレッシュトークン有効期限（時間）
REFRESH_TOKEN_TTL_HOURS=168

# CORS設定
#
# - CORS_ALLOW_ORIGINS: カンマ区切りのリスト (デフォルト: http://localhost:3000)
# - CORS_ALLOW_METHODS: カンマ区切り (デフォルト: GET,POST,PUT,DELETE,OPTIONS)
# - CORS_ALLOW_HEADERS: カンマ区切り (デフォルト: Authorization,Content-Type,X-Requested-With)
# - CORS_ALLOW_CREDENTIALS: true/false (デフォルト: true) Cookieを跨ぐ場合（フロントが別ドメイン/サブドメイン）の設定にtrue
# - CORS_MAX_AGE_SECONDS: 事前検証キャッシュ秒数 (デフォルト: 43200 = 12時間)
#
# AWS配備時
# - Cookieを跨ぐ場合（フロントが別ドメイン/サブドメイン）の設定
#  - AllowCredentials=true（既定で有効）
#  - AllowOriginsはワイルドカード不可。正確なオリジンを列挙してください
#  - 例: CORS_ALLOW_ORIGINS=https://app.example.com,https://admin.example.com
# - CloudFront/ALBを使う場合は、必要に応じてヘッダー転送（Origin, Access-Control-Request-Method, Access-Control-Request-Headers）を有効化し、OPTIONSのパスも許可
# - 本番のCookie属性
#   - フロントのセット側で Secure, SameSite=None, Domain を適切に設定（フロントのセット場所で調整）
CORS_ALLOW_ORIGINS=http://localhost:3000
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOW_HEADERS=Authorization,Content-Type,X-Requested-With
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE_SECONDS=43200  # 12 hours

# Optional simple API key to protect endpoints when deployed.
# Set a strong value in production (and store it in Railway/Render secrets),
# leave empty to disable the check (useful for local development).
# Example:
# API_KEY=change-me-to-a-strong-random-value